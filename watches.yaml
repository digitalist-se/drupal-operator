---
- version: v1alpha2
  group: ozzi.io
  kind: Drupal
  playbook: /opt/ansible/playbook.yaml
  watchDependentResources: true
  maxRunnerArtifacts: 30
  reconcilePeriod: 5s
  manageStatus: False
 # finalizer:
 #    name: finalizer.ozzi.io
  #   role: /opt/ansible/roles/teardown
 #    vars:
  #     state: absent