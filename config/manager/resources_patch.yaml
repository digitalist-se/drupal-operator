---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              memory: "128Mi"
              cpu: "0.1"
            limits:
              memory: "2048Mi"
              cpu: "2"
